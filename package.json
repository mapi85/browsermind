{
  "name": "browsermind",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Dev tooling for the BrowserMind Chrome extension (no build step — tests only)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "optionalDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.60.4"
  }
}
